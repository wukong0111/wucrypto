{
  "name": "wucrypto",
  "version": "0.1.0",
  "type": "module",
  "packageManager": "bun@1.3.13",
  "scripts": {
    "start": "bun run build:css && bun src/index.tsx",
    "dev": "bun --hot src/index.tsx",
    "build:css": "bunx @tailwindcss/cli -i src/styles/input.css -o public/app.css --minify",
    "watch:css": "bunx @tailwindcss/cli -i src/styles/input.css -o public/app.css --watch",
    "typecheck": "bun tsc --noEmit",
    "lint": "bun x biome check --write .",
    "lint:ci": "bun x biome ci .",
    "test": "DATABASE_URL=postgres://wucrypto:wucrypto@localhost:5432/wucrypto_test bun test src",
    "test:e2e": "DATABASE_URL=postgres://wucrypto:wucrypto@localhost:5432/wucrypto_test bunx playwright test",
    "test:e2e:ui": "DATABASE_URL=postgres://wucrypto:wucrypto@localhost:5432/wucrypto_test bunx playwright test --ui",
    "db:generate": "bunx drizzle-kit generate",
    "db:migrate": "bunx drizzle-kit migrate",
    "db:push": "bunx drizzle-kit push",
    "db:studio": "bunx drizzle-kit studio"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.0",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@playwright/test": "^1.61.1",
    "@tailwindcss/cli": "^4.2.0",
    "@types/bun": "^1.2.0",
    "@types/node": "^26.0.0",
    "drizzle-kit": "^0.31.10",
    "playwright": "^1.61.1",
    "tailwindcss": "^4.2.0",
    "typescript": "^5.8.0"
  }
}
